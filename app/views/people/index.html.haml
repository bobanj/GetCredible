#content
  %section#twitter_contacts.content
    %header#page-header

      %h1 People

      %nav.right
        %ul.tabs
          %li.first{:class => people_active_class('index')}
            = link_to 'Pending', people_path, :class => 'js-remote'
          %li{:class => people_active_class('tagged')}
            = link_to 'Tagged', people_tagged_path, :class => 'js-remote'
          %li.last{:class => people_active_class('tagged_you')}
            = link_to 'Tagged You', people_tagged_you_path, :class => 'js-remote'


  #invitation_content
    %section#invitation_contacts.twitter-contacts-list
      %ul
        - @users.each do |user|
          %li
            - if user.active?
              = image_tag user.avatar_url(:medium)
              %span.name= link_to user.name, me_user_path(user)

            - else
              = user.name.presence || user.email
              %span.status

    = will_paginate @users

%aside
  = render 'shared/sidebar_nav'
