$("#write_endorsement_form .loader").addClass('hide');
$("#write_endorsement_description").val('');
$("#write_endorsement_user_tag_id").val('').trigger("liszt:updated");

$("#js-no-endorsements").remove();

var userTagEndorsementContainer = $("#endorse_<%=@endorsement.user_tag_id-%>_container");
var userTagEndorsement = '<%=j(render 'shared/user_tag_endorsement',sad ) %>';
if(userTagEndorsementContainer.length){
  userTagEndorsementContainer.replaceWith(userTagEndorsement);
} else {
  $("#endorsements_list").prepend(userTagEndorsement);
}

