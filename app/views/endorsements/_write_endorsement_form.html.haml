= simple_form_for([@user, endorsement], remote: true, html: {id: 'write_endorsement_form', class:"add-endorsement #{hide ? 'hide' : '' }" }) do |f|
  = f.input :user_tag_id, as: :select, collection: load_user_tags(@user), label: false, input_html:{id: 'write_endorsement_user_tag_id', :'data-placeholder' => "Choose a tag"}
  = f.input :description, label: false, input_html:{id: 'write_endorsement_description', placeholder: "If one word or phrase isn't enough, offer a little insight into why you tagged this profile the way you did", rows: 2}
  = hidden_field_tag :write_endorsement, true
  %span.hint.right
    %span.loader.inline.hide
      = image_tag 'ajax_loader.gif'
    %span#write_endorsement_word_counter 300
    .buttons
      = f.button :submit, "Endorse", class: 'btn small'
      -#%span.inline
      -#  = link_to 'Cancel', '#', id: 'write_endorsement_cancel', class: 'cancel'
