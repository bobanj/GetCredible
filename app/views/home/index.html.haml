#content-wrap
  %header#page-header
    %h1 Latest Activity

  #content
    %section.content
      .feed
        %ul.list-view.unstyled#js_collection_list
          = render 'activities/activities'

      = will_paginate @activity_items

%aside
  = render 'shared/sign'
