$('.loading').hide();
$('#js-email-invitation-form').replaceWith('<%= j(render 'form', resource: User.new, remote: true) %>');
$.getCredible.displayNotification('success', '<%= j I18n.t('devise.invitations.send_instructions', email: resource.email) %>');
