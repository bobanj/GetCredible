$.getCredible.displayNotification('error', 'We could not sent your email at this time.');

$('.loading').hide();
$('#js-email-invitation-form').replaceWith('<%= j(render 'form', message: @message) %>');
