%nav#profile-nav
  .photo
    = link_to image_tag(current_user.avatar_url(:avatar)), me_user_path(current_user), :id => 'user_avatar_sidebar'
    - if show_guide?(current_user, params[:show_guide])
      = link_to image_tag("indicator.png"), nil, id: 'show_guide'

  .my-actions
    %h3
      = "Welcome #{link_to(current_user.short_name, me_user_path(current_user))}".html_safe
    -# .view
      //= link_to 'Settings', edit_user_registration_path
      //&middot;
      -# = link_to 'Edit Profile', edit_user_registration_path
