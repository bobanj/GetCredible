%nav#friend-nav
  = render 'shared/friendship_button', user: user
  %ul
    %li
      = link_to user_following_path(user) do
        %span= user.followings.active.count
        Following
    %li
      = link_to user_followers_path(user) do
        %span= user.followers.active.count
        Followers

  - if user_signed_in? && (user.blank? || user == current_user)
    = link_to 'Invite and Tag Your Contacts', invite_path, class: 'invite'
