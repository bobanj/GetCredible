- #TODO change paths after refactor
%nav
  %ul#global-nav
    %li#gn-logo
      = link_to 'GetCredible', root_path
    - if user_signed_in?
      %li#gn-activity
        = link_to 'Activity feed', home_activity_path
      %li#gn-profile
        = link_to 'Profile', user_path(current_user)
      %li
        = link_to 'DProfile', edit_user_registration_path
      %li
        = link_to 'EInvite', home_invite_email_path
      %li
        = link_to 'TInvite', home_invite_twitter_path
      %li#gn-logout
        = link_to "Logout", destroy_user_session_path
    - else
      %li#gn-login
        = link_to 'Login', new_user_session_path
- #TODO search users form
#global-search
  = form_tag users_path, :id => 'g-search', :method => :get do
    = label_tag :q, "Search"
    = text_field_tag :q, '', :id => 'g-searchtext', :placeholder => 'Search people'
    %button go
