Migration
class AddFieldsToUser < ActiveRecord::Migration
  def change
     TODO still not sure...we may need Profile model to keep this data separate

    # TODO add avatar carrierwave https://github.com/jnicklas/carrierwave
  end
end
